include ../header
<!DOCTYPE html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Explore Dream Discover 📍 #{title}
        link(rel="stylesheet", href="/styles/basePage.css")
        link(rel="shortcut icon"  href="/images/logo.png"  type="image/x-icon")
        link(href="https://fonts.googleapis.com/css2?family=Bellota:ital,wght@1,300&family=Metal+Mania&display=swap" rel="stylesheet")
        link(rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous")


block content
    link(rel="stylesheet", href="/styles/chatapp.css")
    script(src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/2.3.0/socket.io.dev.js")
    input(type="text", name="FriendId" id="friend" placeholder="Enter Friend ID")
    div(id="mario-chat")
        div(id="chat-window")
            div(id ="output")
            div(id="feedback")
        
        h5(id="handle" name="userName")=user.username
        a(id="user" href=user._id)
        p(id="err")
        input(type="text" id ="message" placeholder="Type message"  name="message")
        button(id="send" type = "submit") Send



    script(src="https://cdnjs.cloudflare.com/ajax/libs/axios/0.19.2/axios.js")
    script(src="/js/chatapp.js")